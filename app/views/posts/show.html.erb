<h1><%= @post.title %></h1>

<% @post.sumpoints.each do |sumpoint| %>
  <p>
    * <%= sumpoint.body %>
  </p>
<% end %>

<% if policy(@post).edit? %>
  <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-success' %>
<% end %>