<div class="col-lg-12 text-center">
    <h3 class="section-subheading text-muted">Posts You Are Following</h3>
</div>
<hr>
<% @followed_posts.each do |post| %>
  <%= render partial: 'posts/post', locals: { post: post } %>
<% end %>

